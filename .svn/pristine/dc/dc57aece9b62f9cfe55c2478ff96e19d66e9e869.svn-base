@(formulario: Form[auxiliar])

@import helper._

@implicitFieldConstructor=@{ FieldConstructor(views.html.include.constructor_field_input.render) }

@main("Buscar Profesor") {
	
@if(flash.containsKey("invalid")) {
	<h3 style="color: green">
		<image src="@routes.Assets.at("/public/images","ok.gif")">
		@flash.get("invalid")
	</h3>
} 

<h2> Busqueda de Profesores  </h2>

@form(routes.ProfesorController.mostrar()) {
	<fieldset>
		@inputText(formulario("dni"),'_showConstraints -> false, '_label -> "Ingrese dni del profesor :")
	</fieldset>
	
	
    <input type="submit" value="Buscar"> -
	<a href="@routes.ProfesorController.index()"> <input type="button" value="Cancelar"> </a>
}
    
}